<div class="p-5">
  <h1 class="mb-4 text-center">Portfolio – <%= @sub_category.titleize %></h1>

  <div class="masonry-gallery">
    <% @artworks.each do |artwork| %>
      <% artwork.images.each do |img| %>
        <div class="masonry-item">
          <%= link_to artwork_path(artwork) do %>
            <%= image_tag img.variant(resize_to_limit: [600, 600]), class: "img-fluid", alt: artwork.title %>
          <% end %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
